namespace SMSForwarder;

public static class Program
{
    [STAThread]
    public static void Main(string[] args)
    {
        // Para aplicaciones MAUI en Windows, el punto de entrada se maneja automáticamente
        // Este archivo es solo para satisfacer el requisito del compilador
    }
}